# Система кэширования GAFUS

## 📋 Обзор

В проекте используется многоуровневая система кэширования для оптимизации производительности и поддержки офлайн-режима.

## 🏗️ Архитектура кэширования

```
┌─────────────────────────────────────────────────────────────────┐
│                        КЛИЕНТ (Браузер)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. React Query Cache (In-Memory)                         │  │
│  │    - staleTime: 5 минут                                   │  │
│  │    - gcTime: 10 минут                                     │  │
│  │    - Персистентность: localStorage                        │  │
│  │    - Ключ: "gafus-react-query-cache"                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 2. localStorage                                           │  │
│  │    - Офлайн-состояние (offlineStore)                      │  │
│  │    - Настройки уведомлений                                │  │
│  │    - Паузы тренировок                                     │  │
│  │    - Таймеры                                              │  │
│  │    - Device subscriptions                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 3. IndexedDB (gafus-offline-courses)                      │  │
│  │    - Скачанные курсы (полные данные)                     │  │
│  │    - Медиафайлы (видео, изображения, PDF)                │  │
│  │    - HTML страниц курсов (резервный вариант)             │  │
│  │    - Структура: courses store                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 4. Service Worker Cache API                               │  │
│  │    - gafus-offline-v1: статические chunks, страница      │  │
│  │      офлайна                                             │  │
│  │    - gafus-courses-v1: HTML страниц курсов              │  │
│  │    - sw-localstorage: IndexedDB для SW (localStorage)   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        СЕРВЕР (Next.js)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 5. Next.js Data Cache (unstable_cache)                    │  │
│  │    - React Server Components Cache                        │  │
│  │    - Теги для инвалидации                                 │  │
│  │    - revalidate: время жизни кэша                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 📦 Детальное описание уровней кэширования

### 1. React Query Cache (Клиент)

**Местоположение:** `packages/react-query/src/providers/QueryProvider.tsx`

**Настройки:**
- `staleTime`: 5 минут
- `gcTime`: 10 минут (время хранения в памяти)
- `refetchOnWindowFocus`: false
- `refetchOnReconnect`: true
- `networkMode`: "offlineFirst"

**Персистентность:**
- Хранится в `localStorage` с ключом `"gafus-react-query-cache"`
- Автоматически восстанавливается при загрузке страницы

**Использование:**
```typescript
// Примеры использования
useQuery({ queryKey: ['courses'], queryFn: fetchCourses })
useQuery({ queryKey: ['user', userId], queryFn: fetchUser })
```

**Инвалидация:**
```typescript
queryClient.invalidateQueries({ queryKey: ['courses'] })
```

---

### 2. localStorage (Клиент)

**Использование:**

#### a) Офлайн-состояние
- **Ключ:** `gafus-offline-store` (через Zustand persist)
- **Данные:** статус онлайн/офлайн, очередь синхронизации
- **Файл:** `apps/web/src/shared/stores/offlineStore.ts`

#### b) Настройки уведомлений
- **Ключи:**
  - `notificationsDisabledByUser`
  - `notificationModalShown`
- **Файлы:** `pushStore.ts`, `notificationUIStore.ts`

#### c) Паузы тренировок
- **Ключ:** `training-{courseId}-{day}-{stepIndex}-paused`
- **Данные:** `{ pausedAt: timestamp, timeLeft: number }`
- **Файлы:** `stepStore.ts`, `timerStore.ts`

#### d) Таймеры
- **Ключ:** `timer-{courseId}-{day}-{stepIndex}-{start|end}`
- **Данные:** timestamp начала/окончания
- **Файл:** `cacheManager.ts`

#### e) Device subscriptions
- **Ключ:** `device-subscriptions`
- **Данные:** подписки на push-уведомления
- **Файл:** `packages/webpush/src/device-manager.ts`

---

### 3. IndexedDB (Клиент)

**База данных:** `gafus-offline-courses`
**Версия:** 1
**Store:** `courses`

**Структура данных:**
```typescript
interface OfflineCourse {
  courseId: string;
  courseType: string;
  trainingDays: TrainingDay[];
  mediaFiles: {
    videos: Record<string, Blob>;
    images: Record<string, Blob>;
    pdfs: Record<string, Blob>;
  };
  htmlPages?: {
    listPage?: string;
    dayPages?: Record<number, string>;
  };
  version: number;
  downloadedAt: Date;
}
```

**Индексы:**
- `courseType` (unique: false)
- `version` (unique: false)
- `downloadedAt` (unique: false)

**Операции:**
- `saveOfflineCourse()` - сохранение курса
- `getOfflineCourse(courseId)` - получение по ID
- `getOfflineCourseByType(courseType)` - получение по типу
- `updateOfflineCourse()` - обновление
- `deleteOfflineCourse()` - удаление

**Файл:** `apps/web/src/shared/lib/offline/offlineCourseStorage.ts`

---

### 4. Service Worker Cache API (Клиент)

**Кэши:**

#### a) `gafus-offline-v1`
**Содержимое:**
- Статические chunks Next.js (`/_next/static/`)
- HTML страницы офлайна (`/~offline`)
- Chunks страницы офлайна

**Стратегия:** Cache-First для статики

**Файл:** `apps/web/public/sw.js` (строки 36-73, 114-164)

#### b) `gafus-courses-v1`
**Содержимое:**
- HTML страниц курсов (`/trainings/[courseType]`, `/trainings/[courseType]/[day]`)

**Стратегия:** Network-First с fallback на кэш

**Файл:** `apps/web/public/sw.js` (строки 169-310)

#### c) `sw-localstorage` (IndexedDB)
**Содержимое:**
- Эмуляция localStorage для Service Worker
- Используется для хранения настроек SW

**Файл:** `apps/web/public/sw.js` (строки 498-563)

**Обработка запросов:**
1. **Статические файлы** (`/_next/static/`): Cache-First
2. **Страница офлайна** (`/~offline`): Cache-First с обновлением в фоне
3. **Страницы курсов** (`/trainings/*`): Network-First → Cache → IndexedDB → Fallback
4. **Остальные навигации**: Network-First → Fallback на страницу офлайна

---

### 5. Next.js Data Cache (Сервер)

**Местоположение:** `apps/web/src/shared/lib/actions/cachedCourses.ts`

**Функции кэширования:**

#### a) `getAllCoursesCached`
- **Теги:** `["courses", "courses-all-permanent"]`
- **Revalidate:** 20 минут
- **Ключ:** `["courses-all-permanent"]`
- **Данные:** Все курсы (публичные + приватные) без пользовательских данных

#### b) `getUserCoursesProgressCached(userId)`
- **Теги:** `["courses", "user-progress", "user-{userId}"]`
- **Revalidate:** 5 минут
- **Ключ:** `["user-courses-progress", userId]`
- **Данные:** Прогресс пользователя по курсам

#### c) `getCoursesWithProgressCached(userId)`
- **Теги:** `["courses", "courses-all", "user-{userId}"]`
- **Revalidate:** 5 минут
- **Ключ:** `["courses-all", userId]`
- **Данные:** Все курсы с прогрессом пользователя

#### d) `getFavoritesCoursesCached(userId)`
- **Теги:** `["courses", "courses-favorites", "user-{userId}"]`
- **Revalidate:** 5 минут
- **Ключ:** `["courses-favorites", userId]`
- **Данные:** Избранные курсы пользователя

#### e) `getAuthoredCoursesCached`
- **Теги:** `["courses", "courses-authored"]`
- **Revalidate:** 60 секунд
- **Ключ:** `["courses-authored"]`
- **Данные:** Курсы, созданные пользователем

#### f) `getTrainingDaysCached(type, userId)`
- **Теги:** `["training", "days", "user-{userId}"]`
- **Revalidate:** 5 минут
- **Ключ:** `["training-days", type, userId]`
- **Данные:** Дни тренировок курса

#### g) `getTrainingDayCached(dayId, userId)`
- **Теги:** `["training", "day", "user-{userId}"]`
- **Revalidate:** 5 минут
- **Ключ:** `["training-day", dayId, userId]`
- **Данные:** Конкретный день тренировки

#### h) `getUserTrainingDates(userId)`
- **Теги:** `["achievements", "streaks", "user-{userId}"]`
- **Revalidate:** 5 минут
- **Ключ:** `["user-training-dates", userId]`
- **Данные:** Даты тренировок для серий и достижений

**Инвалидация кэша:**

#### Функции инвалидации:

1. **`invalidateCoursesCache()`**
   - Инвалидирует: `courses`, `courses-all`, `courses-all-permanent`, `courses-favorites`, `courses-authored`
   - Вызывается при создании/обновлении/удалении курсов

2. **`invalidateUserProgressCache(userId)`**
   - Инвалидирует: `user-{userId}`, `user-progress`, `training`, `days`, `courses-favorites`, `courses`, `achievements`, `streaks`
   - Вызывается при изменении прогресса пользователя

3. **`invalidateTrainingDaysCache(courseId?)`**
   - Инвалидирует: `training`, `days`
   - Вызывается при изменении дней тренировок

4. **`revalidatePath(path)`**
   - Инвалидирует кэш страницы по пути
   - Используется для обновления конкретных страниц

**Файлы:**
- `apps/web/src/shared/lib/actions/cachedCourses.ts`
- `apps/web/src/shared/lib/actions/invalidateCoursesCache.ts`
- `apps/web/src/shared/lib/actions/invalidateAllCache.ts`
- `apps/web/src/shared/lib/achievements/getUserTrainingDates.ts`

---

## 🔄 Потоки данных

### Загрузка курсов (онлайн)

```
1. Компонент запрашивает курсы
   ↓
2. Проверка Next.js Data Cache (unstable_cache)
   ├─ Есть в кэше и не устарел → возврат из кэша
   └─ Нет в кэше или устарел → запрос к БД
       ↓
3. Сохранение в Next.js Data Cache
   ↓
4. Отправка на клиент
   ↓
5. Сохранение в React Query Cache
   ↓
6. Отображение в UI
```

### Загрузка курсов (офлайн)

```
1. Компонент запрашивает курсы
   ↓
2. Проверка React Query Cache
   ├─ Есть в кэше → возврат из кэша
   └─ Нет в кэше → проверка IndexedDB
       ↓
3. Проверка IndexedDB (gafus-offline-courses)
   ├─ Курс скачан → возврат из IndexedDB
   └─ Курс не скачан → ошибка/пустой результат
       ↓
4. Отображение в UI
```

### Скачивание курса для офлайна

```
1. Пользователь нажимает "Скачать курс"
   ↓
2. Загрузка данных курса с сервера
   ├─ Структура курса (дни, шаги)
   ├─ Медиафайлы (видео, изображения, PDF)
   └─ HTML страниц курса
       ↓
3. Сохранение в IndexedDB
   ├─ Структура курса → courses store
   ├─ Медиафайлы → mediaFiles (videos, images, pdfs)
   └─ HTML → htmlPages (listPage, dayPages)
       ↓
4. Кэширование chunks через Cache API
   ├─ Извлечение ссылок на chunks из HTML
   └─ Сохранение в gafus-offline-v1
       ↓
5. Сохранение HTML в Service Worker Cache
   └─ gafus-courses-v1
```

### Навигация на страницу курса (офлайн)

```
1. Пользователь переходит на /trainings/[courseType]
   ↓
2. Service Worker перехватывает запрос
   ↓
3. Проверка gafus-courses-v1 (Cache API)
   ├─ Есть HTML → возврат из кэша
   └─ Нет HTML → запрос к клиенту
       ↓
4. Клиент проверяет IndexedDB
   ├─ Есть HTML → отправка в SW
   └─ Нет HTML → fallback HTML
       ↓
5. SW возвращает HTML клиенту
   ↓
6. Next.js загружается на клиенте
   ↓
7. Компонент использует useCachedTrainingDays
   ↓
8. Загрузка данных из IndexedDB
   ↓
9. Отображение страницы
```

### Загрузка медиафайла (офлайн)

```
1. Компонент использует useOfflineMediaUrl(courseType, mediaUrl)
   ↓
2. Проверка IndexedDB
   ├─ Файл найден → создание blob URL
   └─ Файл не найден → использование оригинального URL
       ↓
3. Отображение медиафайла
```

---

## 🏷️ Теги кэширования Next.js

### Общие теги
- `courses` - все операции с курсами
- `courses-all` - все курсы с прогрессом
- `courses-all-permanent` - базовый список всех курсов
- `courses-favorites` - избранные курсы
- `courses-authored` - созданные курсы
- `training` - дни тренировок
- `days` - дни тренировок (дубликат)
- `user-progress` - прогресс пользователя
- `achievements` - достижения
- `streaks` - серии тренировок

### Пользовательские теги
- `user-{userId}` - все данные пользователя
- `user-{userId}-progress` - прогресс пользователя

### Инвалидация по тегам

```typescript
// Инвалидация всех курсов
revalidateTag("courses");

// Инвалидация прогресса пользователя
revalidateTag(`user-${userId}`);
revalidateTag("user-progress");

// Инвалидация дней тренировок
revalidateTag("training");
revalidateTag("days");
```

---

## 🔧 Управление кэшем

### Очистка кэша

**Функция:** `clearAllCache()`  
**Файл:** `apps/web/src/shared/lib/utils/clearAllCache.ts`

**Очищает:**
1. React Query Cache (`queryClient.clear()`)
2. Service Worker Cache (все кэши с префиксом `gafus-`)
3. IndexedDB (все курсы)
4. localStorage (таймеры, паузы, настройки)
5. IndexedDB для SW (`sw-localstorage`)

**Использование:**
```typescript
import { clearAllCache } from "@shared/lib/utils/clearAllCache";

await clearAllCache();
```

---

## 📊 Схема взаимодействия уровней

```
┌─────────────────────────────────────────────────────────────┐
│                    КЛИЕНТСКИЙ УРОВЕНЬ                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  React Query ──┐                                            │
│                │                                            │
│  localStorage ──┼───┐                                        │
│                │   │                                        │
│  IndexedDB ────┼───┼───┐                                    │
│                │   │   │                                    │
│  SW Cache ─────┘   │   │                                    │
│                    │   │                                    │
│                    │   │                                    │
│                    ▼   ▼                                    │
│              ┌─────────────┐                               │
│              │  Компоненты  │                               │
│              └─────────────┘                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                        │
                        │ Server Actions / API Routes
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    СЕРВЕРНЫЙ УРОВЕНЬ                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Next.js Data Cache (unstable_cache)                        │
│         │                                                    │
│         ▼                                                    │
│  Prisma (PostgreSQL)                                        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚙️ Настройки времени жизни кэша

| Тип данных | Revalidate | staleTime | gcTime | Примечание |
|-----------|------------|-----------|--------|------------|
| Все курсы (базовый) | 20 мин | - | - | Редко меняются |
| Курсы с прогрессом | 5 мин | - | - | Прогресс меняется чаще |
| Прогресс пользователя | 5 мин | - | - | Зависит от активности |
| Дни тренировок | 5 мин | - | - | Могут обновляться |
| Избранные | 5 мин | - | - | Зависит от действий пользователя |
| React Query (общий) | - | 5 мин | 10 мин | Клиентский кэш |
| React Query (курсы) | - | 30 сек | 5 мин | Более частые обновления |
| React Query (профиль) | - | 1 мин | 10 мин | Средняя частота |
| React Query (статистика) | - | 5 мин | 15 мин | Редко меняется |

---

## 🚀 Оптимизации

1. **Разделение базовых данных и пользовательских**
   - Базовые курсы кэшируются на 20 минут
   - Прогресс пользователя кэшируется на 5 минут
   - Объединение происходит на клиенте

2. **Офлайн-приоритет**
   - IndexedDB для полных данных курсов
   - Cache API для HTML и chunks
   - localStorage для состояния

3. **Инвалидация по тегам**
   - Группировка связанных данных по тегам
   - Точечная инвалидация при изменениях

4. **Персистентность React Query**
   - Автоматическое восстановление при загрузке
   - Снижение нагрузки на сервер

---

## 📝 Примечания

- Service Worker кэширует только HTML и chunks, данные курсов хранятся в IndexedDB
- HTML страниц сохраняется в IndexedDB как резервный вариант
- Медиафайлы загружаются через blob URL из IndexedDB
- Офлайн-режим работает только для скачанных курсов
- Инвалидация кэша в офлайне ставится в очередь синхронизации
