# Защита видео через HLS

## Обзор

Система защиты видео от скачивания через HLS (HTTP Live Streaming) с токенами доступа.

**Дата внедрения:** Январь 2026  
**Версия:** 1.0

## Проблема

Тренерские видео, загруженные на платформу, были доступны для простого скачивания через браузер (правый клик → Сохранить видео). Это создавало риск кражи контента.

## Решение

Реализована система автоматического транскодирования видео в HLS формат с защитой через signed URLs.

### Ключевые компоненты:

1. **Автоматическое транскодирование** - FFmpeg в worker контейнере
2. **HLS формат** - видео разбивается на сегменты по 6 секунд
3. **Signed URLs** - JWT токены для доступа к манифестам и сегментам
4. **Прокси API** - все запросы к HLS файлам идут через защищённый API
5. **Клиентский плеер** - hls.js для воспроизведения

## Архитектура

### 1. Загрузка и транскодирование

```
Тренер загружает MP4
        ↓
uploadTrainerVideoAction (генерирует videoId)
        ↓
Загрузка в Object Storage: trainers/{trainerId}/videocourses/{videoId}/original.mp4
        ↓
Задача добавляется в очередь video-transcoding
        ↓
Worker скачивает видео
        ↓
FFmpeg транскодирует в HLS (720p или исходное если меньше)
        ↓
Загрузка HLS файлов обратно в Object Storage
        ↓
Обновление БД: hlsManifestPath, transcodingStatus = COMPLETED
        ↓
Удаление original.mp4 (экономия места)
```

### 2. Воспроизведение

```
Пользователь открывает страницу с видео
        ↓
getSignedVideoUrl() генерирует JWT токен (TTL 2 часа)
        ↓
HLSVideoPlayer получает URL манифеста с токеном
        ↓
GET /api/video/{videoId}/manifest?token=<jwt>
        ↓
API проверяет токен и права доступа
        ↓
API скачивает манифест из Object Storage
        ↓
API модифицирует URL сегментов (добавляет токен)
        ↓
Возвращает модифицированный манифест
        ↓
hls.js запрашивает сегменты через /api/video/{videoId}/segment
        ↓
Воспроизведение видео
```

## Структура файлов в CDN

### Новая структура (с HLS):

```
uploads/trainers/{trainerId}/videocourses/{videoId}/
  └── hls/
      ├── playlist.m3u8 (манифест)
      └── segment-000.ts, segment-001.ts, ... (~30-40 файлов)
```

**Преимущества:**
- Все файлы одного видео в одной папке
- Легко удалять (deleteFolderFromCDN)
- Логическая иерархия

### Старая структура (MP4):

```
uploads/trainer-videos/{trainerId}/{uuid}.mp4
```

**Статус:** Устарела. Все новые видео транскодируются в HLS. Старые MP4 файлы удаляются после транскодирования.

## База данных

### Изменения в TrainerVideo:

```prisma
enum TranscodingStatus {
  PENDING      // В очереди на транскодирование
  PROCESSING   // Транскодируется сейчас
  COMPLETED    // Готово
  FAILED       // Ошибка
}

model TrainerVideo {
  // ... существующие поля ...
  
  hlsManifestPath   String?             // trainers/{trainerId}/videocourses/{videoId}/hls/playlist.m3u8
  transcodingStatus TranscodingStatus   @default(PENDING)
  transcodedAt      DateTime?
  transcodingError  String?             // Текст ошибки если провалилось
  
  @@index([transcodingStatus])
}
```

## Пакеты

### @gafus/video-access

Управление доступом к HLS видео:

```typescript
import { getVideoAccessService } from "@gafus/video-access";

const service = getVideoAccessService();

// Генерация токена
const token = service.generateToken({
  videoId: "video-123",
  userId: "user-456",
  ttlMinutes: 120,
});

// Проверка токена
const tokenData = service.verifyToken(token);
```

## API Endpoints

### GET /api/video/[videoId]/manifest

Возвращает HLS манифест с подписанными URL сегментов.

**Query параметры:**
- `token` (required) - JWT токен доступа

**Заголовки ответа:**
```
Content-Type: application/vnd.apple.mpegurl
Cache-Control: no-cache, no-store, must-revalidate
```

### GET /api/video/[videoId]/segment

Возвращает HLS сегмент (`.ts` файл).

**Query параметры:**
- `token` (required) - JWT токен доступа
- `path` (required) - Относительный путь к сегменту

**Заголовки ответа:**
```
Content-Type: video/mp2t
Cache-Control: public, max-age=31536000, immutable
```

## Клиентские компоненты

### HLSVideoPlayer

```typescript
<HLSVideoPlayer
  src={signedManifestUrl}
  controls
  onError={(error) => console.error(error)}
/>
```

**Возможности:**
- Воспроизведение только HLS формата
- Поддержка hls.js для HLS
- Нативная поддержка HLS в Safari
- Показ статуса обработки, если HLS ещё не готов

### VideoTranscodingPlaceholder

```typescript
<VideoTranscodingPlaceholder
  status="PROCESSING"
  error={null}
/>
```

Показывается пока видео транскодируется.

## Worker

### video-transcoding-worker

**Расположение:** `packages/worker/src/video-transcoding-worker.ts`

**Процесс транскодирования:**

1. Скачивание видео из Object Storage
2. Определение разрешения через `ffprobe`
3. Транскодирование через FFmpeg:
   - Если высота >= 720px → downscale до 720p
   - Если высота < 720px → оставить исходное
4. Загрузка HLS файлов в Object Storage
5. Обновление БД
6. Удаление временных файлов и оригинала

**FFmpeg команда:**

```bash
# Для 720p
ffmpeg -i input.mp4 -vf "scale=-2:720" -c:v libx264 -c:a aac \
  -hls_time 6 -hls_playlist_type vod \
  -hls_segment_filename "hls/segment-%03d.ts" \
  -hls_list_size 0 hls/playlist.m3u8

# Для исходного разрешения
ffmpeg -i input.mp4 -c:v libx264 -c:a aac \
  -hls_time 6 -hls_playlist_type vod \
  -hls_segment_filename "hls/segment-%03d.ts" \
  -hls_list_size 0 hls/playlist.m3u8
```

**Concurrency:** 1 (одно транскодирование за раз)  
**Retry:** 3 попытки с exponential backoff

## Миграция существующих видео

### Скрипт миграции

```bash
# Dry-run (без изменений)
pnpm tsx scripts/migrate-videos-to-hls.ts --dry-run

# Миграция 10 видео
pnpm tsx scripts/migrate-videos-to-hls.ts --batch-size=10

# Миграция 50 видео
pnpm tsx scripts/migrate-videos-to-hls.ts --batch-size=50
```

**Что делает:**
1. Находит все видео без `hlsManifestPath`
2. Устанавливает `transcodingStatus = PENDING`
3. Добавляет задачи в очередь транскодирования
4. После транскодирования оригинальные MP4 файлы удаляются

## Безопасность

### Защита от скачивания

1. **HLS формат** - видео разбито на 30-40 файлов вместо одного
2. **Signed URLs** - все запросы требуют валидный JWT токен
3. **Прокси API** - прямой доступ к Object Storage заблокирован
4. **TTL токенов** - токены живут 2 часа
5. **Валидация прав** - проверка что пользователь имеет доступ к видео

### Не абсолютная защита

⚠️ Технически возможно:
- Скачать все сегменты вручную (сложнее чем один файл)
- Записать экран во время воспроизведения

✅ Защищает от:
- Простого скачивания через браузер
- Массового копирования контента
- Распространения прямых ссылок

## Производительность

### Транскодирование

- **Время:** 2-5 минут на видео (зависит от размера)
- **Память:** ~500MB-1GB на видео
- **CPU:** Высокая нагрузка во время транскодирования
- **Диск:** Временные файлы в `/tmp` (~2x размер видео)

### Хранилище

- **До:** 1 видео = 100MB (MP4)
- **После:** 1 видео = ~100MB (HLS 720p) + манифест
- **Экономия:** оригинальное MP4 удаляется после транскодирования

### Воспроизведение

- **Первый кадр:** ~1-2 секунды (загрузка манифеста + первого сегмента)
- **Buffering:** Минимальный (адаптивная загрузка сегментов)
- **Трафик:** Только необходимые сегменты (пользователь смотрит не всё видео)

## Feature Flag

```env
# .env.local
NEXT_PUBLIC_ENABLE_HLS_PROTECTION=true  # Включить HLS
NEXT_PUBLIC_ENABLE_HLS_PROTECTION=false # Отключить HLS (показывает ошибку)
```

При `false` - HLSVideoPlayer показывает ошибку для HLS видео. Воспроизведение MP4 напрямую не поддерживается.

## Мониторинг

### Bull Board

URL: `/admin/queues` (доступ только для ADMIN)

**Метрики:**
- Размер очереди `video-transcoding`
- Активные задачи
- Завершённые задачи
- Провалившиеся задачи

### Логи

```typescript
import { createWorkerLogger } from "@gafus/logger";

const logger = createWorkerLogger("video-transcoding-worker");
```

**Логируется:**
- Начало/завершение транскодирования
- Ошибки FFmpeg
- Размер файлов
- Время выполнения

## Troubleshooting

### Видео застряло в статусе PENDING

**Причина:** Worker не запущен или перегружен

**Решение:**
1. Проверить что worker запущен: `docker ps | grep worker`
2. Проверить логи: `docker logs gafus-worker`
3. Проверить очередь в Bull Board

### Видео в статусе FAILED

**Причина:** Ошибка FFmpeg или недостаточно места

**Решение:**
1. Проверить `transcodingError` в БД
2. Проверить логи worker
3. Повторить транскодирование вручную

### HLS не воспроизводится

**Причина:** Проблема с токеном или CORS

**Решение:**
1. Проверить что токен валиден
2. Проверить CORS настройки в Object Storage
3. Проверить Network tab в DevTools

## CORS настройка (Yandex Object Storage)

```xml
<CORSConfiguration>
  <CORSRule>
    <AllowedOrigin>https://gafus.ru</AllowedOrigin>
    <AllowedOrigin>https://*.gafus.ru</AllowedOrigin>
    <AllowedMethod>GET</AllowedMethod>
    <AllowedMethod>HEAD</AllowedMethod>
    <AllowedHeader>*</AllowedHeader>
    <MaxAgeSeconds>3000</MaxAgeSeconds>
  </CORSRule>
</CORSConfiguration>
```

**Применение через AWS CLI:**

```bash
aws s3api put-bucket-cors \
  --bucket gafus-media \
  --cors-configuration file://cors.xml \
  --endpoint-url=https://storage.yandexcloud.net
```

## Roadmap

### Реализовано ✅

- [x] FFmpeg в Docker worker
- [x] Автоматическое транскодирование
- [x] HLS воспроизведение через hls.js
- [x] Signed URLs (JWT токены)
- [x] API для манифестов и сегментов
- [x] UI для статуса транскодирования
- [x] Скрипт миграции
- [x] Удаление fallback на MP4 воспроизведение

### В разработке 🚧

- [ ] IndexedDB кэширование HLS (для офлайна)
- [ ] Офлайн-воспроизведение HLS
- [ ] Проверка квоты IndexedDB
- [ ] Детальный прогресс скачивания

### Запланировано 📋

- [ ] Множественные качества (360p, 720p, 1080p)
- [ ] Адаптивная передача (ABR)
- [ ] Предпросмотр видео (thumbnail timeline)
- [ ] Субтитры (WebVTT)

## Контакты

**Вопросы:** Telegram @gafus_support  
**Документация:** `/docs/architecture/`
