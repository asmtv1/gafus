# Telegram Bot - Бот для уведомлений и взаимодействия

## 🤖 Описание

Telegram Bot - это Node.js приложение, использующее Telegraf библиотеку для создания бота в Telegram. Бот обеспечивает аутентификацию пользователей через номер телефона, отправку уведомлений и взаимодействие с системой Gafus.

## 🎯 Основные функции

### Аутентификация пользователей
- Подтверждение номера телефона через Telegram
- Связывание Telegram аккаунта с профилем пользователя
- Автоматическая активация аккаунта после подтверждения

### Уведомления
- Push-уведомления о тренировках
- Напоминания о достижениях
- Обновления системы
- Персональные сообщения от тренеров

### Взаимодействие
- Команды для быстрого доступа к функциям
- Интерактивные кнопки и меню
- Обратная связь с пользователями
- Поддержка и помощь

## 🏗️ Архитектура

### Технологический стек
- **Node.js** - серверная платформа
- **TypeScript** - типизированный JavaScript
- **Telegraf** - библиотека для Telegram Bot API
- **Prisma** - ORM для работы с базой данных
- **Queues** - система очередей для фоновых задач

### Структура проекта

```
apps/telegram-bot/
├── bot.ts                    # Основной файл бота
├── package.json              # Зависимости и скрипты
├── tsconfig.json             # Конфигурация TypeScript
└── dist/                     # Скомпилированный код
    └── bot.js                # Скомпилированный бот
```

## 🔧 Конфигурация

### Переменные окружения
```env
# Telegram Bot
TELEGRAM_BOT_TOKEN="your-bot-token"

# База данных
DATABASE_URL="postgresql://..."

# Redis (для очередей)
REDIS_URL="redis://localhost:6379"
```

### Telegraf конфигурация
- **Webhook** - для продакшн окружения
- **Polling** - для разработки
- **Error handling** - централизованная обработка ошибок
- **Middleware** - промежуточное ПО для логирования

## 🚀 Запуск и разработка

### Команды
```bash
# Разработка
pnpm dev                    # Запуск в режиме разработки
pnpm start                  # Продакшн запуск

# Сборка
pnpm build                  # Компиляция TypeScript

# Типизация
pnpm typecheck             # Проверка типов
```

### Режимы работы
- **Development** - polling режим для локальной разработки
- **Production** - webhook режим для продакшн сервера

## 🔐 Аутентификация

### Процесс подтверждения
1. Пользователь отправляет команду `/start`
2. Бот просит отправить номер телефона
3. Пользователь нажимает кнопку "Подтвердить номер"
4. Бот получает контакт и проверяет номер в базе данных
5. При успешной проверке аккаунт активируется

### Код аутентификации
```typescript
// Обработка контакта
if ("contact" in msg && msg.contact) {
  const phone = "+" + msg.contact.phone_number.replace(/\D/g, "");
  
  const user = await prisma.user.findUnique({ where: { phone } });
  
  if (user) {
    await prisma.user.update({
      where: { phone },
      data: {
        telegramId: chatId.toString(),
        isConfirmed: true,
      },
    });
    
    ctx.reply("✅ Вы успешно подтверждены!");
  }
}
```

## 📱 Команды и интерфейс

### Основные команды
- `/start` - начало работы с ботом
- `/help` - помощь и список команд
- `/profile` - информация о профиле
- `/trainings` - список тренировок
- `/achievements` - достижения пользователя

### Интерактивные элементы
- **Кнопки** - для быстрого доступа к функциям
- **Inline клавиатуры** - для навигации
- **Контактная кнопка** - для подтверждения номера
- **Меню команд** - список доступных команд

### Пример интерфейса
```typescript
// Кнопка подтверждения номера
ctx.reply("Нажмите кнопку ниже, чтобы отправить номер телефона:", {
  reply_markup: {
    keyboard: [
      [
        {
          text: "📲 Подтвердить номер",
          request_contact: true,
        },
      ],
    ],
    resize_keyboard: true,
    one_time_keyboard: true,
  },
});
```

## 🔔 Система уведомлений

### Типы уведомлений
- **Тренировки** - напоминания о тренировках
- **Достижения** - уведомления о новых достижениях
- **Прогресс** - обновления прогресса
- **Системные** - важные обновления системы

### Отправка уведомлений
```typescript
// Отправка уведомления пользователю
async function sendNotification(telegramId: string, message: string) {
  try {
    await bot.telegram.sendMessage(telegramId, message);
  } catch (error) {
    console.error("Ошибка отправки уведомления:", error);
  }
}
```

### Массовые уведомления
- Отправка всем пользователям
- Отправка по группам пользователей
- Отправка по фильтрам
- Планировщик уведомлений

## 🔄 Интеграция с системой

### База данных
- Связывание Telegram ID с пользователем
- Обновление статуса подтверждения
- Синхронизация данных профиля

### Очереди задач
- Асинхронная отправка уведомлений
- Обработка массовых рассылок
- Планирование отложенных сообщений

### API интеграция
- Получение данных пользователей
- Синхронизация с веб-приложением
- Обновление статистики

## 🛡️ Безопасность

### Валидация данных
- Проверка номера телефона
- Валидация Telegram ID
- Защита от спама

### Обработка ошибок
- Централизованная обработка ошибок
- Логирование всех операций
- Graceful shutdown

### Rate Limiting
- Ограничение частоты запросов
- Защита от злоупотреблений
- Блокировка подозрительных пользователей

## 📊 Мониторинг и логирование

### Логирование
- Все действия пользователей
- Ошибки и исключения
- Производительность бота
- Статистика использования

### Метрики
- Количество активных пользователей
- Частота использования команд
- Успешность доставки уведомлений
- Время отклика бота

### Мониторинг
- Health checks
- Error tracking
- Performance monitoring
- Alert система

## 🧪 Тестирование

### Типы тестов
- Unit тесты для функций
- Integration тесты с базой данных
- E2E тесты для пользовательских сценариев
- Mock тесты для Telegram API

### Тестовые данные
- Тестовые пользователи
- Mock Telegram API
- Тестовые уведомления
- Симуляция ошибок

## 🚀 Развертывание

### Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
CMD ["npm", "start"]
```

### Переменные окружения
- `TELEGRAM_BOT_TOKEN` - токен бота
- `DATABASE_URL` - строка подключения к БД
- `REDIS_URL` - строка подключения к Redis
- `NODE_ENV` - окружение (development/production)

### Webhook настройка
- Настройка webhook URL
- SSL сертификат
- Проверка подписи запросов
- Обработка обновлений

## 📈 Производительность

### Оптимизация
- Кэширование данных пользователей
- Батчинг запросов к API
- Асинхронная обработка
- Connection pooling

### Масштабирование
- Горизонтальное масштабирование
- Load balancing
- Redis для сессий
- Database sharding

## 🔧 Разработка и расширение

### Добавление новых команд
```typescript
bot.command('newcommand', (ctx) => {
  ctx.reply('Новая команда!');
});
```

### Middleware
```typescript
bot.use((ctx, next) => {
  console.log('Middleware executed');
  return next();
});
```

### Обработчики событий
```typescript
bot.on('text', (ctx) => {
  // Обработка текстовых сообщений
});
```
